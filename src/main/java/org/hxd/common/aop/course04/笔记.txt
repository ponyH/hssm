1、环绕通知
    可以获得目标方法的全部控制权(是否执行、执行前(后)、参数、返回值等它都能操作)

2、可以通过控制methodInvocation.proceed()是否执行，来控制目标方法是否执行

3、methodInvocation.proceed()方法的返回值，就是目标方法的返回值，可以通过修改此返回值，修改目标方法的返回值
    偷梁换柱

4、环绕通知的本质是一个拦截器
    拦截方法，并获取方法的一切控制权